# MetaBlog — Frontend

React + Vite + SASS frontend for the blog app. See the root [README.md](../README.md) for the full setup and API reference.

```
npm install
cp .env.example .env
npm run dev
```

Runs at `http://localhost:3000`, expects the GraphQL backend at `http://localhost:4000`.
